## Python Practice Exercise — Fundamentals Mix

Create a small Python program called **“Student Admission Checker”** that does the following:

### Requirements

1. Ask the user for:

   * Full name
   * Age
   * Exam score
   * Whether they have a valid ID card (`yes` or `no`)

2. Use **typecasting** where necessary.

3. Create variables for all collected data.

4. Use **mathematical operators** to:

   * Add 5 bonus points to the exam score
   * Calculate how many years remain before the student turns 30

5. Use **comparison operators** to check:

   * If the final score is greater than or equal to 70
   * If the age is at least 18

6. Use **logical operators** to determine if the student qualifies:

   * A student qualifies only if:

     * final score is at least 70
     * age is 18 or above
     * they have a valid ID card

7. Use an **if…elif…else** statement to print:

   * `"Excellent candidate"` if score is 90 and above
   * `"Qualified candidate"` if score is between 70 and 89
   * `"Not qualified"` otherwise

8. Use an **identity operator** (`is` or `is not`) somewhere meaningful in your code.

9. Display a final summary like:

   * Name
   * Original score
   * Final score
   * Qualification status

---

## Bonus Challenge

Add another condition that checks:

* If the candidate is below 18 but scored above 95, print:
  `"Special consideration candidate"`
